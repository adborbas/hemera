import Foundation

extension Mortar {
    /// Fixed sizes for control panel interactive elements.
    public enum ControlPanelSize {
        /// Width of sliders and control stacks in control panels.
        public static let controlWidth: CGFloat = 120
    }
}
